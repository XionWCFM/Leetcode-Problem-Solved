/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var search = function(nums, target) {
    let [left,right,middle] = [0,nums.length-1,nums.length/2]
    return nums.indexOf(target)
    
};